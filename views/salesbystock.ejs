<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>sales by stock</title>
    <style>
        body {
          background-color: #f8f9fa;
        }
        .report-container {
          background: #fff;
          padding: 20px;
          border-radius: 10px;
          box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .report-title {
          font-size: 20px;
          font-weight: 600;
          margin-bottom: 20px;
        }
        .filter-row {
          display: flex;
          flex-wrap: nowrap;
          align-items: flex-end;
          gap: 15px;
          margin-bottom: 20px;
        }
        .filter-row .form-group {
          flex: 1;
          min-width: 160px;
        }
        .btn-purple {
          background-color: #8E44AD;
          color: #fff;
        }
        .btn-orange {
          background-color: orange;
          color: #fff;
        }
        .table thead {
          background-color: #2566af;
          color: #fff;
        }
        .table tfoot td {
          font-weight: bold;
          text-align: right;
        }
      </style>

</head>
<body>

  <div class="container">
        <div style="box-shadow: 0 4px 8px rgba(0,0,0,0.1); border-radius: 10px; padding: 20px; background: white;">
            <div style="font-size: 20px; font-weight: 600; margin-bottom: 20px;">Sales Report (Lot Wise)</div>
            <form>
                <div class="filter-row">
                  <div class="form-group">
                    <label for="lotNo" class="form-label">Lot Numbers</label>
                    <select id="lotNo" class="form-select">
                      <option>All Lot Numbers</option>
                      <!-- Dynamically filled -->
                    </select>
                  </div>
                  <div class="form-group">
                    <label for="partner" class="form-label">Partner</label>
                    <select id="partner" class="form-select">
                      <option>All Partners</option>
                      <!-- Dynamically filled -->
                    </select>
                  </div>
                  <div class="form-group">
                    <label for="dateFrom" class="form-label">Date From</label>
                    <input type="date" id="dateFrom" class="form-control" value="<%= new Date().toISOString().slice(0, 10) %>">
                  </div>
                  <div class="form-group">
                    <label for="dateTo" class="form-label">Date Upto</label>
                    <input type="date" id="dateTo" class="form-control" value="<%= new Date().toISOString().slice(0, 10) %>">
                  </div>
                  <div class="form-group d-flex gap-1">
                    <button type="submit" class="btn btn-purple">Search</button>
                    <button type="reset" class="btn btn-orange">Reset</button>
                    <button type="button" class="btn btn-purple">Print</button>
                  </div>
                </div>
              </form>
            <div class="table-responsive">
                <table class="table table-bordered">
                    <thead style="background-color: #2566af; color: white;">
                        <tr>
                            <th>Sr No</th>
                            <th>Lot No</th>
                            <th>Partner</th>
                            <th>Date</th>
                            <th>Customer Name</th>
                            <th>Amount</th>
                            <th>Due Date</th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Dynamic data goes here -->
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="5" style="text-align: right; font-weight: bold;">Total:</td>
                            <td style="font-weight: bold;">0.000</td>
                            <td></td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>

    
   

    


</body>
</html>